# テストの実行
```bash
$ uv run pytest {options...}
```

# コード変更後の標準フロー

コードを変更した場合は、以下の順序で検証と修正を行うこと。

1. `uv run ruff check` を実行する。
2. Ruff の指摘をすべて修正する。例外種別などの仕様変更が妥当な場合は、既存実装を維持するために lint を抑制せず、実装とテストコードを合わせて更新する。
3. `uv run ruff check` を再実行し、`All checks passed!` になることを確認する。
4. `uv run pytest` を実行し、すべてのテストが通ることを確認する。
5. テスト失敗を修正した場合は、Ruff と pytest の両方を再実行して最終確認する。
